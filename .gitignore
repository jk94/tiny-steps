# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
# ...except the tracked template (mirrors config.example.yml's convention).
!.env.example

# Logs
*.log

# OS
.DS_Store

# Claude Code internal state (worktrees, session metadata)
.claude/

# Prisma SQLite dev database
apps/backend/prisma/dev.db
apps/backend/prisma/dev.db-journal

# Runtime data (e.g. SQLite volume mounted in docker-compose)
data/

# Real config (only config.example.yml is tracked)
config.yml

# claude.ai/design sync — transient build/verification state (see
# .design-sync/config.json and docs/design-system/ for the durable parts)
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
apps/frontend/types/
