# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
# ...except the tracked template (mirrors config.example.yml's convention).
!.env.example

# Logs
*.log

# OS
.DS_Store

# Claude Code internal state (worktrees, session metadata)
.claude/

# Prisma SQLite dev database
apps/backend/prisma/dev.db
apps/backend/prisma/dev.db-journal

# Runtime data (e.g. SQLite volume mounted in docker-compose)
data/

# Real config (only config.example.yml is tracked)
config.yml
