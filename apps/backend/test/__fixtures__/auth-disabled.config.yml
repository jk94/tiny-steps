# Used by auth.e2e-spec.ts to verify that LocalAuthEnabledGuard hides
# register/login (404) while refresh/logout/me stay reachable when
# auth.local.enabled is false.
server:
  port: 3000
database:
  provider: sqlite
auth:
  local:
    enabled: false
  oidc:
    providers: []
